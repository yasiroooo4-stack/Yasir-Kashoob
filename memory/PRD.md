# نظام ERP لمزرعة الألبان - PRD

## نظرة عامة
نظام ERP متكامل لإدارة مزرعة الألبان يشمل:
- إدارة الموردين واستلام الحليب
- إدارة الموارد البشرية والرواتب
- إدارة المبيعات والعملاء
- النظام المالي والخزينة
- إدارة المستودعات المتكاملة
- نظام التنبيهات الذكي
- نظام طلبات السلف والمصاريف
- بوابة الموردين الإلكترونية
- نظام مزامنة البصمة من أجهزة ZKTeco
- نظام تحديد الموقع الجغرافي (Geofencing)
- **إدارة عقود وفواتير المشاريع (جديد)**

## Tech Stack
- **Frontend:** React + Vite + Tailwind CSS + Shadcn/UI
- **Backend:** FastAPI (Python)
- **Database:** MongoDB
- **Languages:** Arabic (primary) + English

## الميزات المُنجزة

### 28 يناير 2026 - ميزات المخزون المتقدمة وإصلاحات

#### 1. إصلاح أنواع الأعلاف في بوابة الموردين (P0) ✅
- **المشكلة:** أنواع الأعلاف كانت hardcoded ولا تظهر من قاعدة البيانات
- **الحل:** 
  - إنشاء API جديد `/api/supplier-portal/feed-types` بدون authentication
  - تحديث Frontend لحساب السعر بشكل صحيح: `price_per_unit / kg_per_unit`
  - الآن تظهر 7 أنواع أعلاف مع الأسعار الصحيحة

#### 2. واجهات المخزون المتقدمة (P0) ✅
تم إنشاء 4 مكونات جديدة في `/app/frontend/src/components/inventory/`:

##### BatchTracking.jsx - تتبع الدفعات واللوتات
- عرض وإدارة دفعات المنتجات مع تواريخ الإنتاج والصلاحية
- تنبيهات للدفعات قريبة الانتهاء
- فلترة حسب المستودع والمنتج
- إنشاء دفعات جديدة

##### CycleCount.jsx - الجرد الدوري
- إنشاء عمليات جرد دوري جديدة
- إدخال الكميات الفعلية ومقارنتها بالنظام
- حساب الفروقات تلقائياً
- اعتماد الجرد وتعديل المخزون

##### ReturnsManagement.jsx - إدارة المرتجعات
- مرتجعات من العملاء وإلى الموردين
- تتبع أسباب الإرجاع
- الموافقة على المرتجعات وتعديل المخزون
- إحصائيات شاملة

##### InventoryValuation.jsx - تقييم المخزون
- تقييم المخزون بطرق مختلفة (FIFO، المتوسط المرجح)
- أعلى 10 منتجات قيمة
- معدل دوران المخزون مع التحليل والتوصيات
- أيام المخزون والتصنيف (ممتاز/جيد/متوسط/منخفض)

#### 3. APIs المخزون المتقدم
- `GET/POST /api/inventory-advanced/batches` - إدارة الدفعات
- `GET /api/inventory-advanced/batches/expiring` - الدفعات منتهية الصلاحية
- `GET/POST /api/inventory-advanced/cycle-counts` - الجرد الدوري
- `PUT /api/inventory-advanced/cycle-counts/{id}/item/{item_id}` - تحديث عنصر الجرد
- `PUT /api/inventory-advanced/cycle-counts/{id}/approve` - اعتماد الجرد
- `GET/POST /api/inventory-advanced/returns` - المرتجعات
- `PUT /api/inventory-advanced/returns/{id}/approve` - الموافقة على المرتجع
- `GET /api/inventory-advanced/valuation` - تقييم المخزون
- `GET /api/inventory-advanced/turnover` - معدل الدوران

#### 4. إصلاح النصوص المقطوعة في الجداول (P1) ✅
- **المشكلة:** النصوص الطويلة تُقطع بـ `truncate` class ولا يمكن قراءتها
- **الحل:** إنشاء مكون `ExpandableText.jsx` قابل لإعادة الاستخدام
- **الصفحات المُحدّثة:**
  - Treasury.jsx
  - HR.jsx (5 مواضع)
  - Finance.jsx
  - Legal.jsx
  - MaterialIssue.jsx
  - ProjectInvoices.jsx
  - TasksManagement.jsx

### 25 يناير 2026 - الجلسة الثالثة

#### 1. إضافة نوع الحليب والمركز لبوابة الموردين (P0) ✅
- تحديث API `/api/supplier-portal/milk-receptions` لإضافة:
  - `milk_type_ar` / `milk_type_en` (نوع الحليب بالعربي والإنجليزي)
  - `center_name` / `center_name_en` (اسم المركز)
- تحديث جدول سجل التوريدات في بوابة الموردين
- إصلاح استخدام حقل `reception_date` بدلاً من `date`

#### 2. نظام عقود المشاريع (P0) ✅
- **الموقع:** تبويب "العقود" في صفحة إدارة المشاريع
- نموذج بيانات العقد يشمل:
  - اسم المقاول/المورد، هاتف، بريد إلكتروني
  - قيمة العقد، تاريخ البداية والنهاية
  - شروط الدفع، نطاق العمل، الشروط والأحكام
- تتبع المدفوع والمتبقي من قيمة العقد
- حالات العقد: مسودة، نشط، مكتمل، ملغي
- APIs جديدة:
  - `POST /api/projects/contracts` - إنشاء عقد
  - `GET /api/projects/{project_id}/contracts` - جلب عقود المشروع
  - `PUT /api/projects/contracts/{contract_id}` - تحديث عقد

#### 3. نظام فواتير المشاريع مع مراحل الموافقة (P0) ✅
- **الموقع:** تبويب "الفواتير" في صفحة إدارة المشاريع
- نموذج الفاتورة يشمل:
  - ربط بالمشروع والعقد (اختياري)
  - نوع الفاتورة: مرحلة أو دفعة جزئية
  - المبلغ، الوصف، تاريخ الاستحقاق
- **مراحل الموافقة الثلاثية:**
  1. موافقة مسؤول المشاريع
  2. موافقة المالية
  3. موافقة المدير العام
- بعد جميع الموافقات → "جاهزة للصرف"
- إمكانية رفض الفاتورة في أي مرحلة
- عند الصرف → تحديث المبلغ المدفوع في العقد تلقائياً
- **طباعة الفاتورة PDF** مع:
  - شعار المروج للألبان
  - تفاصيل المشروع والفاتورة
  - حالة الموافقات وتواريخها
  - معلومات الصرف
- APIs جديدة:
  - `POST /api/projects/invoices` - إنشاء فاتورة
  - `GET /api/projects/{project_id}/invoices` - جلب فواتير المشروع
  - `PUT /api/projects/invoices/{id}/approve/project-manager` - موافقة مسؤول المشاريع
  - `PUT /api/projects/invoices/{id}/approve/finance` - موافقة المالية
  - `PUT /api/projects/invoices/{id}/approve/gm` - موافقة المدير العام
  - `PUT /api/projects/invoices/{id}/reject` - رفض الفاتورة
  - `PUT /api/projects/invoices/{id}/pay` - صرف الفاتورة

#### 4. التحقق من إعادة تعيين بيانات المخازن (P1) ✅
- تم التحقق من عمل API `/api/admin/reset-data/warehouse_management`
- يحذف البيانات من: warehouses, warehouse_products, warehouse_stock, warehouse_items, warehouse_solutions, warehouse_consumption

### 25 يناير 2026 - الجلسة الثانية

#### 1. تحديث PDF مشتريات الأعلاف (P0) ✅
- تصدير PDF من 3 صفحات:
  - **الصفحة 1:** طلب الشراء الكامل مع الأسعار والإجمالي
  - **الصفحة 2:** طلب الشراء بدون أسعار (للتوزيع)
  - **الصفحة 3:** سجل توريدات الحليب للمورد مع ملخص الرصيد
- إضافة معلومات الشركة الكاملة (CR NO, VAT, P.O BOX)
- إضافة ملخص (إجمالي الحليب، إجمالي الأعلاف، صافي الرصيد)

#### 2. نظام تحديد الموقع الجغرافي - Geofencing (P1) ✅
- **الموقع:** تبويب "المواقع" في صفحة إعدادات النظام
- إعدادات Geofencing مع 3 خيارات:
  - تفعيل/تعطيل تحديد الموقع
  - منع الدخول من مواقع غير مصرحة (وضع الحماية)
  - السماح بالدخول بدون موقع
- إضافة وحذف المواقع المسموح بها (اسم، إحداثيات، نصف القطر)
- زر "استخدام موقعي الحالي" لتسهيل الإضافة
- سجل تسجيلات الدخول مع حالة الموقع
- تحقق من الموقع قبل تسجيل الدخول
- عرض حالة الموقع في صفحة تسجيل الدخول (ناجح/محظور/غير متوفر)
- APIs جديدة:
  - `GET /api/system/geofence-settings` - جلب إعدادات Geofencing
  - `POST /api/system/geofence-settings` - تحديث إعدادات Geofencing
  - `POST /api/auth/check-location` - التحقق من صلاحية الموقع

#### 2. نظام مزامنة البصمة (P1) ✅
- **الموقع:** تبويب "البصمة" في صفحة إعدادات النظام
- تحميل برنامج المزامنة لسطح المكتب (ملف ZIP)
- تعليمات الاستخدام ومتطلبات التشغيل
- إحصائيات المزامنة (عدد الأجهزة، آخر مزامنة، عدد السجلات)
- سجل عمليات المزامنة

#### 3. إصلاح ترجمة الصلاحيات (P1) ✅
- إضافة ترجمة عربية لـ `warehouse_approve_issue` → "الموافقة على طلبات الصرف"

### 25 يناير 2026 - الجلسة الأولى

#### 1. بوابة الموردين (P0) ✅
- **الرابط:** `/supplier-portal`
- تسجيل دخول بكود المورد وكلمة المرور الافتراضية (0000)
- عرض الرصيد الحالي وإجمالي الكمية الموردة
- سجل توريدات الحليب الشهري مع التصفية حسب الشهر والسنة
- طلب تحويل رصيد إلى أعلاف
- إرسال رسائل واستفسارات للإدارة
- تغيير كلمة المرور
- استرجاع كلمة المرور عبر رقم الهاتف

#### 2. إصلاح إعادة تعيين بيانات المخازن (P0) ✅
- إصلاح خلل في API `/api/admin/reset-data/warehouse_management`
- الآن يستهدف المجموعات الصحيحة

#### 3. تحسين تسجيل دخول بوابة الموردين (P0) ✅
- إضافة دعم لكلمة المرور الافتراضية "0000" لجميع الموردين

#### 4. صلاحيات الجدولة (P1) ✅
- صلاحيات موجودة في الباكند والفرونتند

---

### 22 يناير 2026 - تحسينات كبيرة على النظام

#### 1. نظام طلبات السلف والمصاريف (P0) ✅
- زر "طلب سلفة" جديد في الـ Header
- نموذج طلب يدعم: سلفة، مصروفات، استرداد مصاريف
- مسار موافقة: الموارد البشرية ← المالية
- تبويب جديد في صفحة HR لإدارة الطلبات

#### 2. تحسين نموذج طلب الإجازة (P0) ✅
- إضافة حقل "الموظف البديل" (اختياري)
- إضافة حقل "تحويل الصلاحيات إلى" (اختياري)

#### 3. إعادة تنظيم صفحة الموارد البشرية (P0) ✅
- نقل "الموافقة على البدلات" إلى صفحة HR
- حذف تبويبات: العطلات، الورديات، الإضافي
- إضافة تبويب "السلف والمصاريف"

#### 4. إنشاء حسابات للموظفين (P0) ✅
- API جديد: `/api/admin/create-employee-accounts`
- تم إنشاء 57 حساب للموظفين
- اسم المستخدم: رقم الموظف (EMP...)
- كلمة المرور الافتراضية: 0000

#### 5. صلاحيات جديدة (P0) ✅
- تعديل جدول الموظفين (`employees_edit`)
- تعديل جدول السائقين (`drivers_edit`)
- تسجيل مهمة (`register_task`)
- موافقة HR/المالية على السلف

#### 6. تتبع تسجيل الدخول والمواقع المسموحة (P0) ✅
- تحديد الموقع الجغرافي عند تسجيل الدخول (GPS)
- تسجيل عنوان IP والوقت
- إمكانية تحديد مواقع مسموحة على الخريطة
- تنبيه عند تسجيل الدخول من موقع غير مصرح
- سجل كامل لتسجيلات الدخول مع تفاصيل الموقع

#### 7. نظام كاميرات Hikvision (P1) ✅
- دعم كامل لـ Hikvision ISAPI
- تبويب Hik-Connect للاتصال السحابي
- عرض البث المباشر
- البحث في التسجيلات والتشغيل
- تصدير مقاطع الفيديو
- إشعارات الأحداث (حركة، اقتحام)

#### 8. إصلاح ربط الإجازات بالحضور (P0) ✅
**المشكلة:** عند الموافقة على إجازة سنوية:
- سجلات الحضور لم تكن تُنشأ/تُحدث بشكل صحيح
- الخطأ: `uuid4 is not defined`
- تنسيق التاريخ كان غير متوافق مع قاعدة البيانات

**الحل:**
1. تصحيح استخدام `uuid.uuid4()` بدلاً من `uuid4()`
2. استخدام `strftime("%Y-%m-%d")` بدلاً من `isoformat()` للتوافق
3. إضافة logging تفصيلي لتتبع العمليات
4. تحسين منطق البحث عن السجلات الموجودة

**النتيجة:**
- ✅ سجلات الحضور تُنشأ تلقائياً بحالة `leave`
- ✅ رصيد الإجازات يُخصم من الموظف
- ✅ سجل كامل للخصومات يُحفظ في `leave_balance_log`

**الملفات المُعدّلة:**
- `/app/backend/server.py` (endpoint: `/api/hr/leave-requests/{id}/approve`)

---

### 16 يناير 2026 - إصلاح خطأ طلب الإجازة

#### إصلاح خطأ تقديم طلب الإجازة (P0) ✅
**المشكلة:** عند تقديم طلب إجازة كان يظهر خطأ:
```
Error: Objects are not valid as a React child (found: object with keys {type, loc, msg, input, url})
```

**السبب:** 
1. الـ Frontend كان يرسل `days_requested` لكن الـ Backend يتوقع `days_count`
2. معالجة الأخطاء كانت تحاول عرض كائن خطأ Pydantic كنص

**الحل:**
1. تصحيح اسم الحقل من `days_requested` إلى `days_count`
2. إزالة الحقول الزائدة (`notes`) من الطلب
3. تحسين معالجة الأخطاء لعرض رسائل نصية واضحة

**الملفات المُعدّلة:**
- `/app/frontend/src/components/LeaveRequestButton.jsx`

---

### 15 يناير 2026 - الجزء الخامس

#### زر طلب إجازة جديد في Header ✅
- ✅ مكون `LeaveRequestButton` جديد
- ✅ موقع: بين "طلب عذر" و "طلب رسالة"
- ✅ لون: أخضر فاتح (bg-green-50)
- ✅ أيقونة: CalendarDays

#### نموذج طلب الإجازة ✅
| الحقل | الوصف |
|-------|-------|
| الموظف | اختيار للمدير، تلقائي للموظف |
| رصيد الإجازات | يُعرض في صندوق أزرق |
| نوع الإجازة | 9 أنواع |
| من تاريخ | منتقي تاريخ |
| إلى تاريخ | منتقي تاريخ |
| مدة الإجازة | حساب تلقائي بالأيام |
| السبب | textarea |
| ملاحظات | حقل نصي |

**أنواع الإجازات:**
1. إجازة سنوية (annual)
2. إجازة مرضية (sick)
3. إجازة طارئة (emergency)
4. إجازة بدون راتب (unpaid)
5. إجازة أمومة (maternity)
6. إجازة أبوة (paternity)
7. إجازة حج (hajj)
8. إجازة زواج (marriage)
9. إجازة عزاء (bereavement)

#### تنظيف أنواع الرسائل ✅
تم حذف "طلب إجازة" من LetterRequestButton. الأنواع المتبقية:
1. شهادة راتب
2. شهادة استمرارية راتب
3. شهادة عمل
4. شهادة خبرة
5. خطاب مهمة
6. خطاب عدم ممانعة

#### إصلاح زر التحديث في صفحة الحضور (P1) ✅
- ✅ مؤشر تحميل (spinner) أثناء التحديث
- ✅ رسالة نجاح "تم تحديث البيانات بنجاح"
- ✅ الزر معطل أثناء التحميل (`disabled={refreshingAttendance}`)

### 15 يناير 2026 - الأجزاء السابقة

#### تعديل وحذف استلامات الحليب ✅
- APIs: PUT و DELETE /api/milk-receptions/{id}
- صلاحيات: milk_reception_edit, milk_reception_delete

#### استيراد بيانات Ekomilk ✅
- دعم ملفات .xls القديمة
- 1082 سجل، 187 مورد جديد

#### منطق الإجازة في الرواتب ✅
- حالة `leave` = يوم حضور مدفوع

#### صفحة تسجيل الدخول ✅
- شعار Almorooj Dairy المتحرك

#### إعدادات الحساب في Header ✅
- تعديل الملف الشخصي
- تغيير كلمة المرور

#### دمج الصفحات في تبويبات ✅
- التقارير: 6 تبويبات
- إعدادات النظام: 7 تبويبات

## هيكل Header

```
┌──────────────────────────────────────────────────────────────────────────┐
│  [إحصائيات] [طلب عذر] [طلب إجازة] [طلب رسالة] [EN/AR] [User Menu]      │
└──────────────────────────────────────────────────────────────────────────┘
                              ↑
                        زر جديد (أخضر)
```

## API Endpoints

### طلبات الإجازة
```
GET    /api/hr/leave-requests              - قائمة الطلبات
POST   /api/hr/leave-requests              - إنشاء طلب جديد
PUT    /api/hr/leave-requests/{id}/approve - الموافقة على طلب
PUT    /api/hr/leave-requests/{id}/reject  - رفض طلب
```

### استلام الحليب
```
GET    /api/milk-receptions              
POST   /api/milk-receptions              
PUT    /api/milk-receptions/{id}         - تعديل
DELETE /api/milk-receptions/{id}         - حذف
POST   /api/milk-receptions/import       - استيراد
```

---

### 22 يناير 2026 (جلسة 2) - إصلاحات واجهة المستخدم

#### 1. إصلاح عرض أيام الإجازة الأسبوعية (P0) ✅
**المشكلة:** صفحة جدولة الموظفين كانت تعرض أيام الإجازة كـ "الخميس, الجمعة" بدلاً من "الجمعة, السبت"

**السبب:** 
- البيانات في قاعدة البيانات كانت `[4, 5]` (الخميس والجمعة حسب فهرس الأيام)
- المطلوب هو `[5, 6]` (الجمعة والسبت)

**الحل:**
- تحديث جميع سجلات الموظفين (58 موظف) من `[4, 5]` إلى `[5, 6]`
- قاعدة البيانات: `milk_erp.hr_employees`

**النتيجة:** ✅ الآن تظهر "الجمعة, السبت" بشكل صحيح

---

#### 2. إصلاح حساب الرواتب - مشكلة أيام الغياب (P0) ✅
**المشكلة:** ياسر كان يظهر لديه 4 أيام غياب بشكل خاطئ

**السبب الجذري:**
- استخدام `weekday()` بدلاً من `isoweekday()`
- `weekday()` يعطي: 0=الاثنين، 4=الجمعة، 5=السبت
- `isoweekday()` يعطي: 1=الاثنين، 5=الجمعة، 6=السبت
- البيانات المخزنة `[5, 6]` تتوافق مع `isoweekday()` فقط

**الحل:**
- تغيير جميع استخدامات `weekday()` إلى `isoweekday()`
- تحديث القيم الافتراضية من `[4, 5]` إلى `[5, 6]` في جميع أنحاء الكود

**النتيجة:** ✅ ياسر الآن: 23 يوم حضور، **0 أيام غياب**، 350 ر.ع صافي

---

#### 3. إعادة تنظيم صفحة الإعدادات - التبويبات (P0) ✅
**المشكلة:** 8 تبويبات في `grid-cols-7` مما أدى إلى ظهورها في سطرين

**الحل:**
- تغيير التصميم إلى `flex flex-wrap` للسماح بالتمدد التلقائي
- تصغير حجم النصوص وإخفائها على الشاشات الصغيرة

**النتيجة:** ✅ جميع التبويبات في سطر واحد منظم

---

#### 4. إضافة ميزة تصفير البيانات (P0) ✅
**طلب المستخدم:** إضافة زر تصفير لكل قسم (للمدير فقط)

**الأقسام المتاحة للتصفير:**
1. الحضور - حذف جميع سجلات الحضور والانصراف
2. المشاريع - حذف جميع المشاريع والمهام
3. المخزون - حذف جميع بيانات المخزون
4. حركات المخازن - حذف جميع حركات المستودعات
5. المنتجات - حذف جميع المنتجات

**الأمان:**
- متاح فقط لمدير النظام (role: admin)
- يتطلب كتابة اسم القسم للتأكيد قبل الحذف
- تحذير واضح بأن العملية لا يمكن التراجع عنها

**الملفات المُنشأة/المُعدّلة:**
- `/app/frontend/src/pages/SystemSettings.jsx` - إضافة تبويب "تصفير" ومكون `DataResetSettings`
- `/app/backend/server.py` - إضافة endpoint `POST /api/admin/reset-data/{section_id}`

**النتيجة:** ✅ تبويب جديد باللون الأحمر في إعدادات النظام

---

## المهام المعلقة

### P0 - تم إنجازه ✅
- [x] إصلاح عرض أيام الإجازة الأسبوعية (الجمعة والسبت)
- [x] إعادة تنظيم صفحة الإعدادات (التبويبات في سطر واحد)
- [x] إصلاح حساب الرواتب (استخدام isoweekday بدلاً من weekday)
- [x] إضافة ميزة تصفير البيانات (9 أقسام)
- [x] إضافة تقرير تفصيلي للرواتب لكل موظف
- [x] واجهة تحديد جدول العمل للموظفين موجودة وتعمل
- [x] تصدير تقرير الراتب التفصيلي إلى PDF
- [x] تصدير كشف رواتب جميع الموظفين إلى Excel/CSV
- [x] بوابة الموردين `/supplier-portal`
- [x] إضافة صلاحيات جدولة الموظفين والسائقين
- [x] إصلاح تصفير إدارة المخازن
- [x] الترجمة الإنجليزية لصفحة هيكل الرواتب

### P1 - قيد التنفيذ
- [x] واجهة الموافقة على طلبات السلف موجودة وتعمل
- [ ] فلترة قائمة الموظفين في "الأجر الإضافي" - (الطلبات تأتي من سجلات الحضور تلقائياً)
- [ ] تكامل SMS للتنبيهات
- [ ] إعادة هيكلة server.py

### P2 - قادم
- [ ] تكامل المستودعات مع المبيعات
- [ ] تكامل Ekomilk Scale عبر RS232

## بيانات الاختبار
- Admin: `yasir` / `admin123`
- الموظفين: رقم الموظف / `0000`

## نتائج الاختبارات
- ✅ **100%** iteration_14.json - الموافقة على البدلات
- ✅ **100%** iteration_15.json - دمج الصفحات
- ✅ **100%** iteration_16.json - صفحة الدخول
- ✅ **93%** iteration_17.json - استيراد Ekomilk
- ✅ **100%** iteration_18.json - تعديل/حذف استلامات
- ✅ **100%** iteration_19.json - زر طلب إجازة، إصلاح زر التحديث
- ✅ **100%** تقديم طلب الإجازة - اختبار يدوي
- ✅ **100%** إصلاح أيام الإجازة الأسبوعية - اختبار يدوي (Screenshot)
- ✅ **100%** تنظيم صفحة CCTV - اختبار يدوي (Screenshot)
- ✅ **100%** إصلاح حساب الرواتب (ياسر: 0 غياب) - اختبار يدوي (curl + Screenshot)
- ✅ **100%** تبويبات صفحة الإعدادات - اختبار يدوي (Screenshot)
- ✅ **100%** ميزة تصفير البيانات (9 أقسام) - اختبار يدوي (Screenshot)
- ✅ **100%** تقرير الراتب التفصيلي - اختبار يدوي (Screenshot)
- ✅ **100%** واجهة جدولة الموظفين - اختبار يدوي (Screenshot)
- ✅ **100%** تصدير PDF للتقرير التفصيلي - اختبار يدوي (Screenshot)

## آخر تحديث: 26 يناير 2026

### 26 يناير 2026 - إصلاحات نظام الصلاحيات والثيم

#### 1. إصلاح ظهور الصلاحيات بعد تسجيل الدخول (P0) ✅
**المشكلة:** الصلاحيات تُضاف للموظف لكن لا تظهر له بعد تسجيل الدخول

**السبب:**
- كود تسجيل الدخول كان يبحث عن الصلاحيات باستخدام `employee_id` من جدول `users`
- بعض المستخدمين لديهم `employee_id` يحتوي على كود الموظف (EMP...) بدلاً من UUID
- جدول `user_permissions` يستخدم UUID الموظف الفعلي

**الحل:**
- تعديل endpoint تسجيل الدخول (`/api/auth/login`) ليبحث عن الموظف بعدة طرق:
  1. البحث بـ `employee_id` كـ UUID
  2. البحث بـ `employee_id` كـ `employee_code`
  3. البحث بـ `username` كـ `employee_code`
- بعد إيجاد الموظف، يتم جلب الصلاحيات باستخدام UUID الصحيح

**الملفات المُعدّلة:**
- `/app/backend/server.py` (endpoint: `/api/auth/login` سطور 417-497)

**نتائج الاختبار:**
- ✅ الموظف EMP201802 يحصل على صلاحية `dashboard_view` بعد تسجيل الدخول
- ✅ الموظف EMP202554 يحصل على 17 صلاحية HR بعد تسجيل الدخول

---

#### 2. إصلاح إعدادات الثيم لتكون خاصة بكل مستخدم (P0) ✅
**المشكلة:** عند تغيير الثيم/المظهر من مستخدم، يتغير لجميع المستخدمين

**السبب:**
- إعدادات الثيم كانت تُحفظ في `localStorage` فقط
- `localStorage` مشترك لجميع المستخدمين على نفس المتصفح

**الحل:**
1. تحديث نموذج `UserAppearanceSettings` لإضافة حقول جديدة:
   - `app_theme`: لون الثيم (default, ocean, forest, sunset, royal, rose, dark, slate)
   - `dark_mode`: الوضع الداكن (true/false)

2. تحديث API إعدادات المستخدم:
   - `GET /api/user/settings`: يُرجع الإعدادات الشخصية
   - `PUT /api/user/settings`: يحفظ الإعدادات في قاعدة البيانات

3. تحديث الواجهة الأمامية:
   - `ThemeSettings.jsx`: يستخدم API بدلاً من localStorage فقط
   - `Layout.jsx`: يحمّل إعدادات الثيم من الخادم عند تسجيل الدخول

**الملفات المُعدّلة:**
- `/app/backend/models/all_models.py` - إضافة `app_theme` و `dark_mode`
- `/app/backend/server.py` - تحديث GET/PUT user settings
- `/app/frontend/src/pages/ThemeSettings.jsx` - استخدام API
- `/app/frontend/src/components/Layout.jsx` - تحميل الثيم من الخادم

**نتائج الاختبار:**
- ✅ المدير يحفظ ثيم "ocean" → يبقى "ocean" حتى بعد تغيير موظف آخر
- ✅ الموظف يحفظ ثيم "forest" → يبقى "forest" مستقلاً عن المدير
- ✅ كل مستخدم له `user_id` مختلف في `user_settings`

---

### 27 يناير 2026 - تحسينات نظام المستودعات

#### 1. مواقع التخزين الهرمية (P0) ✅
**الموقع:** تبويب "مواقع التخزين" في صفحة إدارة المستودعات

**الميزات المُنجزة:**
- **العرض الهرمي للمخازن:**
  - عرض المخازن حسب المركز (6 مراكز: زيك، حجيف، غدو، طاقة، ثمريت، مرباط)
  - هيكل ثلاثي المستويات: مخزن خارجي ← مخزن داخلي ← مخزن فرعي
  - الترتيب الصحيح: المخازن الخارجية أولاً ثم الداخلية
  - عرض عدد المخازن الفرعية لكل مخزن
  - تمييز أنواع المخازن بالألوان (أزرق للخارجي، أخضر للداخلي)
  - عرض تصنيفات المخازن (مختبر، صيانة، تنظيف، أعلاف، معدات، مستلزمات)
  - علامة خاصة للمخازن المبردة

- **زر "تهيئة المخازن":**
  - إنشاء الهيكل الافتراضي للمخازن لجميع المراكز
  - يُنشئ 54 مخزن (9 لكل مركز × 6 مراكز)
  - API: `POST /api/warehouse/warehouses/initialize-all`

- **نافذة "إضافة مخزن جديد" المحسّنة:**
  - اختيار المركز من قائمة منسدلة
  - اختيار نوع المخزن (خارجي/داخلي)
  - اختيار المخزن الأب (لإنشاء الهيكل الهرمي)
  - اختيار تصنيف المخزن (عام، مختبر، صيانة، إلخ)
  - خيار "مخزن مبرد / تحكم بالحرارة"

#### 2. نظام الأصول الثابتة (P0) ✅
**الموقع:** تبويب "الأصول الثابتة" في صفحة إدارة المستودعات

**الميزات المُنجزة:**
- **إحصائيات الأصول:**
  - إجمالي الأصول
  - الأصول النشطة
  - الأصول في الصيانة
  - الأصول التي تحتاج صيانة

- **جدول الأصول:**
  - رمز الأصل (يُنشأ تلقائياً)
  - الاسم والنوع والفئة
  - الموقع (المخزن والمركز)
  - الحالة (نشط، في الصيانة، مُتقاعد، إلخ)
  - الحالة الفنية (ممتاز، جيد، مقبول، سيء)
  - أزرار الإجراءات (عرض، نقل)

- **فلاتر متقدمة:**
  - البحث بالاسم أو الرمز
  - الفلترة حسب الفئة
  - الفلترة حسب الحالة

- **نافذة "إضافة أصل جديد":**
  - اسم الأصل ونوعه
  - الفئة (أصول ثابتة، مستهلكات، قطع غيار)
  - الماركة والموديل
  - الرقم التسلسلي
  - اختيار المخزن والموقع
  - سعر الشراء والحالة الفنية
  - ملاحظات

- **نقل الأصول بين المخازن:**
  - API: `POST /api/warehouse/fixed-assets/{id}/transfer`
  - تسجيل تاريخ حركة الأصل

**APIs الجديدة:**
- `GET /api/warehouse/warehouses/by-center` - جلب المخازن حسب المركز
- `POST /api/warehouse/warehouses/initialize-all` - تهيئة المخازن الافتراضية
- `GET /api/warehouse/fixed-assets` - جلب الأصول الثابتة
- `GET /api/warehouse/fixed-assets/stats` - إحصائيات الأصول
- `POST /api/warehouse/fixed-assets` - إضافة أصل جديد
- `GET /api/warehouse/fixed-assets/{id}` - عرض تفاصيل أصل
- `POST /api/warehouse/fixed-assets/{id}/transfer` - نقل أصل

**الملفات المُعدّلة:**
- `/app/frontend/src/pages/WarehouseManagement.jsx` - إضافة مكون StorageLocationsSection وتحسين FixedAssetsSection
- `/app/backend/routes/warehouse_routes.py` - APIs للمخازن والأصول

**نتائج الاختبار:**
- ✅ Backend: 15/15 اختبار ناجح (100%)
- ✅ Frontend: جميع اختبارات الواجهة ناجحة (100%)
- ✅ ملف الاختبار: `/app/backend/tests/test_warehouse_storage_assets.py`

---

#### 3. إصلاح عدم ظهور صفحات المبيعات والمشتريات للموظفين (P0) ✅
**المشكلة:** الموظف Salim (EMP202119) لديه صلاحيات المبيعات والمشتريات لكن الصفحات لا تعمل

**السبب:**
- الـ `ProtectedRoute` في App.js كان يستخدم `allowedDepartments` فقط
- الصلاحيات (`allowedPermissions`) لم تكن مُعرّفة للصفحات الرئيسية
- الموظف في قسم `purchasing` لكن الصفحات تتطلب `sales` أو `procurement`

**الحل:**
1. إضافة `allowedPermissions` لجميع الـ Routes المحمية في `App.js`:
   - `/sales` → `sales_view`, `sales_create`, `sales_edit`
   - `/procurement` → `purchases_view`, `purchases_create`, `purchases_edit`, `purchases_approve`
   - `/feed-purchases` → `purchases_view`, `purchases_create`, `purchases_edit`
   - وباقي الصفحات الأخرى...

2. إضافة `purchasing` لقائمة الأقسام المسموحة لصفحة `/procurement`

**الملفات المُعدّلة:**
- `/app/frontend/src/App.js` - إضافة `allowedPermissions` للـ Routes
- `/app/frontend/src/components/Layout.jsx` - إضافة `purchasing` للمشتريات

**نتائج الاختبار:**
- ✅ الموظف EMP202119 يستطيع الوصول لصفحة المبيعات
- ✅ الموظف EMP202119 يستطيع الوصول لصفحة المشتريات

---

## نتائج الاختبارات
- ✅ **100%** iteration_20.json - نظام الصلاحيات المركزي
- ✅ **100%** iteration_21.json - إصلاح الصلاحيات والثيم (8/8 backend, 100% frontend)
- ✅ **100%** iteration_22.json - مواقع التخزين الهرمية والأصول الثابتة (15/15 backend, 100% frontend)


---

### 2 فبراير 2026 - تحسينات قالب الطباعة

#### 1. تغيير لون قالب الطباعة إلى البني الفاتح ✅
**المتطلبات:** تغيير لون القالب من الأخضر/الأزرق إلى البني الفاتح

**الألوان الجديدة:**
- `#8B5A2B` - البني الفاتح الأساسي (العناوين، الحدود، الترويسة)
- `#A67C52` - البني الفاتح الثانوي (التدرجات)
- `#D2B48C` - البني الفاتح جداً (الخلفيات، الحدود)
- `#faf5f0` - خلفية الجداول بدلاً من `#f5f5f5`

**الألوان المُستبدَلة:**
- `#1a5f7a` (أزرق) → `#8B5A2B`
- `#2d8659` (أخضر) → `#A67C52`

**الملفات المُعدّلة:**
- `/app/frontend/src/pages/HR.jsx` - دالة `printDocument` (السطر 1073-1075)
- `/app/frontend/src/pages/Operations.jsx` - دالة `handlePrintMilkDelivery` (السطر 198-200)

#### 2. دعم التوقيعات الإلكترونية ✅
**المتطلبات:** إضافة إمكانية عرض التوقيعات الإلكترونية في نماذج الطباعة

**الحل:**
- إنشاء دالة مساعدة `createSignatureBox(label, signatureUrl)` في HR.jsx (السطر 1047-1064)
- إذا تم توفير URL للتوقيع، يتم عرض الصورة
- إذا لم يتم توفير URL، يظهر مربع "مكان التوقيع"

**CSS Classes:**
- `.e-signature` - خانة التوقيع الفارغة
- `.e-signature-present` - خانة التوقيع مع صورة

**الدوال المُحدّثة لدعم التوقيعات:**
- `handlePrintLetter` - يدعم `employee_signature`, `manager_signature`, `hr_signature`, `gm_signature`
- `handlePrintWarning` - يدعم نفس الحقول
- `handlePrintExcuse` - يدعم نفس الحقول
- `handlePrintExternalWork` - يدعم نفس الحقول
- `handlePrintAdvanceRequest` - يدعم `employee_signature`, `finance_signature`, `gm_signature`

#### 3. نموذج طباعة "إيصال توصيل الحليب" للسائقين ✅
**المتطلبات:** إنشاء نموذج طباعة جديد لتوثيق توصيل الحليب

**الموقع:** صفحة العمليات `/operations` → تبويب "مهام السائقين"

**محتوى النموذج:**
1. **ترويسة الشركة** - شعار ومعلومات الشركة
2. **عنوان الوثيقة** - "إيصال توصيل الحليب - Milk Delivery Receipt"
3. **معلومات السائق** - الاسم، رقم السيارة، نوع المركبة
4. **معلومات النقل** - التاريخ، الوقت، نوع الحليب
5. **مربع الكمية** - عرض بارز للكمية بالتر
6. **قسم المسار** - من (المركز) ← إلى (الوجهة)
7. **الملاحظات** - إذا وُجدت
8. **التوقيعات** - توقيع السائق، توقيع المستلم، توقيع مسؤول العمليات

**أنواع الحليب المدعومة:**
- `camel_milk` - حليب إبل
- `cow_milk` - حليب أبقار
- `sheep_milk` - حليب أغنام

**الملفات المُعدّلة:**
- `/app/frontend/src/pages/Operations.jsx`:
  - إضافة `Printer` icon من lucide-react (السطر 16)
  - دالة `handlePrintMilkDelivery` (السطر 191-515)
  - عمود "طباعة" في جدول مهام السائقين (السطر 887)
  - زر طباعة لكل مهمة (السطر 919-929)

**نتائج الاختبار:**
- ✅ تم التحقق من جميع الميزات عبر مراجعة الكود
- ✅ ملف الاختبار: `/app/test_reports/iteration_25.json`

---

## المهام المعلقة والأولويات

### مشاكل معلقة (P0-P1)
1. **(P0)** فشل مزامنة جهاز البصمة - `sync_manager_gui.py`
2. **(P1)** فقدان بعض بيانات الحضور (يناير 2026) - يحتاج استرداد
3. **(P1)** تحسين تصفية الموظفين في "الأجر الإضافي"

### مهام قادمة
1. **(P1)** ميزة "النسخ الاحتياطي" للمهام - قد تحتاج توضيح من المستخدم
2. **(P2)** إعادة هيكلة `server.py` (>16,000 سطر)
3. **(P2)** إعادة هيكلة `HR.jsx` (>6,000 سطر)

---

## بيانات الاختبار
| المستخدم | الدور | كلمة المرور |
|----------|-------|-------------|
| yasir | admin | admin1111 |
| 2015 | supplier | 0000 |
| EMP202348 | employee | - |


---

### 2 فبراير 2026 - ميزة التوقيعات الإلكترونية للموظفين ✅

#### الوصف
تم إضافة نظام متكامل لإدارة التوقيعات الإلكترونية للموظفين، مع دعم التوقيع التلقائي في نماذج الطباعة.

#### APIs الجديدة

| Endpoint | الوصف |
|----------|-------|
| `POST /api/hr/employees/{id}/signature` | رفع التوقيع الإلكتروني |
| `DELETE /api/hr/employees/{id}/signature` | حذف التوقيع |
| `GET /api/hr/signatures/authorized` | جلب التوقيعات المعتمدة |

#### قيود رفع التوقيع
- **الأنواع المسموحة:** PNG, JPG, JPEG, GIF, WEBP
- **الحجم الأقصى:** 2MB
- **مسار الحفظ:** `/app/uploads/signature_{employee_id}_{uuid}.{ext}`

#### التوقيعات المعتمدة (تُستخدم تلقائياً)
| النوع | المناصب المعترف بها |
|-------|---------------------|
| `gm` | المدير العام، General Manager، GM، CEO |
| `hr` | مدير الموارد البشرية، HR Manager، HR Director |
| `finance` | المدير المالي، Finance Manager، CFO |

#### التكامل مع الطباعة
دالة `createSignatureBox(label, signatureUrl, signatureType)` تبحث تلقائياً عن التوقيع المعتمد إذا لم يتم توفير URL.

**النماذج المدعومة:**
- نموذج الإجازة: توقيع الموظف، المدير، HR
- الرسائل الرسمية: HR، المدير العام
- الإنذارات: الموظف، المدير، HR
- طلبات العذر: الموظف، المدير، HR
- العمل الخارجي: الموظف، المدير، HR
- السلف والمصاريف: الموظف، المالية، المدير العام

#### الملفات المُعدّلة
- `/app/backend/server.py` - 3 APIs جديدة (~130 سطر)
- `/app/backend/models/all_models.py` - حقل `signature_url` في `EmployeeBase`
- `/app/frontend/src/pages/HR.jsx`:
  - `authorizedSignatures` state
  - واجهة رفع التوقيع في نموذج الموظف
  - تحديث `createSignatureBox`
  - جلب التوقيعات في `fetchData`

#### نتائج الاختبار
- ✅ Backend: 11/11 اختبارات ناجحة
- ✅ Code Review: جميع الميزات موجودة
- ملف الاختبار: `/app/test_reports/iteration_26.json`


---

### 2 فبراير 2026 - تحسينات مهام السائقين ونماذج الطباعة ✅

#### 1. ترجمة نماذج الطباعة تلقائياً ✅
تم تحديث جميع دوال الطباعة لتدعم اللغتين العربية والإنجليزية:

**الدوال المُحدّثة في HR.jsx:**
- `printDocument` - القالب الأساسي مع دعم RTL/LTR
- `handlePrintLetter` - نموذج الإجازة والرسائل الرسمية
- `handlePrintWarning` - نموذج الإنذارات
- `handlePrintExcuse` - نموذج طلب العذر
- `handlePrintExternalWork` - نموذج العمل الخارجي
- `handlePrintAdvanceRequest` - نموذج السلف والمصاريف

**الدوال المُحدّثة في Operations.jsx:**
- `handlePrintMilkDelivery` - إيصال توصيل الحليب

**التغييرات:**
- `direction: rtl/ltr` حسب اللغة
- `text-align: right/left` حسب اللغة
- ترجمة جميع العناوين والتسميات
- تاريخ بصيغة محلية (`ar-SA` / `en-US`)

#### 2. ربط مهام السائقين بأسطول المركبات ✅
تم تحديث حقل "رقم السيارة" ليعرض قائمة من أسطول المركبات:

**الميزات:**
- عرض المركبات المتاحة (`status: available/operational`)
- عرض معلومات المركبة (رقم اللوحة، الماركة، الموديل، النوع)
- تعبئة تلقائية لنوع المركبة عند الاختيار
- خيار "إدخال يدوي" للمركبات غير المسجلة

**الكود:**
```jsx
<Select onValueChange={(v) => {
  const selectedVehicle = vehicles.find(veh => veh.plate_number === v);
  setDriverTaskForm({
    ...driverTaskForm, 
    vehicle_plate: v,
    vehicle_type: selectedVehicle?.vehicle_type || ...
  });
}}>
```

#### 3. أيقونات الحيوانات بدلاً من البترول ✅
تم استبدال أيقونة `Fuel` بأيقونات SVG مخصصة للحيوانات:

| نوع الحليب | الأيقونة | اللون |
|-----------|---------|-------|
| حليب إبل | 🐪 Camel SVG | Amber |
| حليب أبقار | 🐄 Cow SVG | Blue |
| حليب أغنام | 🐑 Sheep SVG | Pink |

#### 4. زر حذف مهام السائقين ✅
تم إضافة زر حذف في عمود "الإجراءات":

**الميزات:**
- تأكيد قبل الحذف (`window.confirm`)
- رسالة نجاح بعد الحذف
- تحديث الجدول والإحصائيات بعد الحذف

**API:**
- `DELETE /api/operations/driver-tasks/{task_id}`
- يتطلب صلاحية: `admin` أو `operations_manager`

#### نتائج الاختبار
- ✅ Backend: 9/9 اختبارات ناجحة
- ✅ Frontend: جميع الميزات تعمل
- ملف الاختبار: `/app/test_reports/iteration_27.json`


---

### 5 فبراير 2026 - إصلاح طباعة حالة طلب الإجازة ✅

#### المشكلة
عند طباعة نموذج طلب الإجازة، لم تكن حالة الطلب (موافق/مرفوض/قيد الانتظار) تظهر في المستند المطبوع.

#### السبب
في دالة طباعة طلب الإجازة، كان يتم إنشاء كائن `leaveData` بدون حقل `status`:

```jsx
const leaveData = {
  letter_type: 'leave_form',
  employee_name: req.employee_name,
  // ... باقي الحقول
  purpose: req.reason || ''
  // ❌ حقل status مفقود!
};
```

#### الحل
تم إضافة حقل `status: req.status` إلى كائن `leaveData`:

```jsx
const leaveData = {
  letter_type: 'leave_form',
  employee_name: req.employee_name,
  // ... باقي الحقول
  purpose: req.reason || '',
  status: req.status  // ✅ تمت الإضافة
};
```

#### الملفات المُعدّلة
- `/app/frontend/src/pages/HR.jsx` - السطر 2811: إضافة `status: req.status`

#### نتائج الاختبار
- ✅ حالة "مرفوض" تظهر بشكل صحيح (لون أحمر)
- ✅ حالة "موافق" تظهر بشكل صحيح (لون أخضر)
- ✅ حالة "قيد الانتظار" تظهر بشكل صحيح (لون أصفر)
- ملف الاختبار: `/app/test_reports/iteration_28.json`

---

## المهام المعلقة (محدّثة)

### مشاكل معلقة (P0-P1)
1. **(P0)** ~~طباعة حالة طلب الإجازة~~ ✅ **تم الإصلاح**
2. **(P1)** فشل مزامنة جهاز البصمة - `sync_manager_gui.py`
3. **(P1)** فقدان بعض بيانات الحضور (يناير 2026) - يحتاج استرداد
4. **(P2)** تحسين تصفية الموظفين في "الأجر الإضافي"
5. **(P2)** دقة رواتب الموظفين غير الإداريين

### مهام إعادة الهيكلة (عاجلة جداً)
1. **(P0)** إعادة هيكلة `server.py` (>16,000 سطر) → تقسيم إلى routers
2. **(P0)** إعادة هيكلة `HR.jsx` (~6,000 سطر) → تقسيم إلى components

### مهام قادمة
1. **(P1)** ميزة "النسخ الاحتياطي" للمهام - قد تحتاج توضيح من المستخدم
