# ZKTeco Fingerprint Sync Manager
# مدير مزامنة أجهزة البصمة ZKTeco

## 📦 محتويات الحزمة

| الملف | الوصف |
|-------|-------|
| `sync_manager_gui.py` | **تطبيق سطح المكتب** - واجهة رسومية سهلة |
| `network_sync.py` | مزامنة عبر الشبكة (بدون ملفات MDB) |
| `sync_agent.py` | مزامنة ملفات MDB |
| `config_sample.json` | ملف تكوين نموذجي |

---

## 🚀 البدء السريع

### 1. تثبيت المتطلبات
```bash
pip install requests pyzk
```

### 2. تشغيل التطبيق الرسومي
```bash
python sync_manager_gui.py
```

---

## 📱 تطبيق سطح المكتب (sync_manager_gui.py)

واجهة رسومية سهلة الاستخدام تتضمن:
- ✅ إضافة وإدارة أجهزة البصمة
- ✅ اختبار الاتصال بالأجهزة
- ✅ مزامنة يدوية أو تلقائية
- ✅ سجل العمليات المباشر
- ✅ دعم ملفات MDB والاتصال المباشر

### لقطة الشاشة:
```
┌─────────────────────────────────────────────────┐
│  مدير مزامنة البصمات - ZKTeco Sync Manager      │
├─────────────────────────────────────────────────┤
│ إعدادات الاتصال بالنظام                          │
│ عنوان API: [________________________] [حفظ]     │
│ اسم المستخدم: [___________]                      │
│ كلمة المرور: [***********]                       │
├─────────────────────────────────────────────────┤
│ أجهزة البصمة                                     │
│ ┌─────────────────────────────────────────┐     │
│ │ جهاز الرئيسي - 192.168.1.100:4370      │     │
│ │ جهاز الفرعي - 192.168.1.101:4370       │     │
│ └─────────────────────────────────────────┘     │
│ [➕ إضافة] [✏️ تعديل] [🗑️ حذف] [🔌 اختبار]     │
├─────────────────────────────────────────────────┤
│        [🔄 مزامنة الآن]  ☐ مزامنة تلقائية       │
├─────────────────────────────────────────────────┤
│ سجل العمليات                                     │
│ [12:30:45] ✅ تم الاتصال بالجهاز                 │
│ [12:30:50] 📊 تم جلب 150 سجل حضور               │
│ [12:30:55] ✅ تمت المزامنة بنجاح                 │
└─────────────────────────────────────────────────┘
```

---

## 📡 الاتصال المباشر بالشبكة (network_sync.py)

للاتصال مباشرة بأجهزة ZKTeco عبر الشبكة (بدون ملفات MDB):

### اختبار الاتصال بجهاز:
```bash
python network_sync.py --test 192.168.1.100
```

### مزامنة مرة واحدة:
```bash
python network_sync.py --config config.json
```

### تشغيل كخدمة مستمرة:
```bash
python network_sync.py --daemon --config config.json
```

---

## 📁 مزامنة ملفات MDB (sync_agent.py)

إذا كنت تفضل استخدام ملفات MDB:

```bash
python sync_agent.py --mdb C:/ZKTeco/att2000.mdb -u yasir -p password
```

---

## ⚙️ ملف التكوين (config.json)

```json
{
  "api_url": "https://milk-erp.preview.emergentagent.com",
  "username": "yasir",
  "password": "your_password",
  "sync_interval": 3600,
  "devices": [
    {
      "name": "جهاز البصمة الرئيسي",
      "ip": "192.168.1.100",
      "port": 4370,
      "location": "المدخل الرئيسي"
    }
  ],
  "mdb_paths": [
    "C:/ZKTeco/att2000.mdb"
  ]
}
```

---

## 📋 المتطلبات

### Windows:
```bash
pip install requests pyzk
```

### Linux:
```bash
pip install requests pyzk
sudo apt-get install python3-tk mdbtools
```

---

## 🔧 استكشاف الأخطاء

### فشل الاتصال بالجهاز
1. تأكد من أن الجهاز متصل بالشبكة
2. تحقق من عنوان IP والمنفذ (4370 افتراضياً)
3. تأكد من عدم وجود جدار حماية يمنع الاتصال

### خطأ في المصادقة
1. تحقق من اسم المستخدم وكلمة المرور
2. تأكد من صحة عنوان API

### مكتبة pyzk غير موجودة
```bash
pip install pyzk
```

### مكتبة tkinter غير موجودة (Linux)
```bash
sudo apt-get install python3-tk
```

---

## 📞 الدعم

للمساعدة، تواصل مع فريق الدعم الفني.
